"""Packaged RuleSpec overlay specs.

Each ``*.yaml`` here is an ``axiom_oracles.rulespec_overlay.v1`` spec loaded by
name through :func:`axiom_oracles.bridges.rulespec_overlay.load_overlay_spec`.
The directory is a package so the specs ship in the wheel alongside the code
that consumes them.
"""
